# GitLab personal access token — required scopes: api, read_repository
# Create at: GitLab → Preferences → Access Tokens
GITLAB_API_TOKEN=

# GitLab API base URL for self-hosted CE instances
# Default assumes GitLab running on localhost (Docker Compose)
GITLAB_API_URL=http://localhost/api/v4
